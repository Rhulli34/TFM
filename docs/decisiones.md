# Registro de decisiones

Formato: una línea por decisión.
- YYYY-MM-DD — Qué se decidió — Por qué.
- 2026-06-22 — F0 completada: precios=yfinance, noticias=Finnhub company-news endpoint, corpus=Financial PhraseBank v1.0 (takala/financial_phrasebank en HuggingFace, descargado como zip y parseado directamente porque el loading script HF tiene URI rota). Python 3.12 en .venv (pandas 2.2.x porque pandas 3.x tiene DLLs bloqueadas por WDAC en esta máquina); datasets pinneado a 3.6.0.
- 2026-06-23 — F1 completada: EDA sobre sentences_allagree (2264 frases, 5 duplicados exactos quitados, 0 nulos). Desbalance notable: neutral 61.4%, positive 25.2%, negative 13.4%. Split 70/15/15 estratificado, semilla 42, sobre 2259 muestras únicas → train=1581, val=339, test=339. Config elegida: sentences_allagree (mayor calidad; sentences_50agree ofrece 4846 muestras si en F2 se necesita más datos). Figuras en reports/figures/, métricas en reports/results/eda_corpus.json.
- 2026-06-23 — Entorno GPU verificado: RTX 5060 (Blackwell, sm_120, 8 GB VRAM), driver 596.49, CUDA driver 13.2. Instalado torch==2.11.0+cu128 + transformers==5.12.1 + accelerate==1.14.0 + evaluate==0.4.6. Matmul 1024x1024 en GPU OK. Compute capability (12,0) soportada por cu128.
- 2026-06-23 — F2 transformer completado: microsoft/deberta-v3-base (NO ProsusAI/finbert: ya fine-tuneado en datos que se solapan con Financial PhraseBank → data leakage). 10 épocas, lr=2e-5, batch=16, float32 (fp16/bf16 causan dtype mismatch en la pérdida ponderada), class-weighted CE, max_length=128, selección por val macro-F1. Test macro-F1=0.981 (+12.5 pp sobre baseline). F1 negative=0.979, recall=1.0 (cero misses). Entrenamiento: 3 min GPU.
- 2026-06-23 — F2 baseline completado: TF-IDF (1-2 grams, sublinear_tf, min_df=2) + LinearSVC (C=0.5, class_weight=balanced) gana con macro-F1=0.856 en test (LR queda en 0.855). F1 por clase en test: negative=0.814, neutral=0.932, positive=0.822. Negative es el cuello de botella esperado. Listón fijado para FinBERT y LLM zero-shot. Modelo guardado en models/, figuras en reports/figures/, métricas en reports/results/baseline.json.
- 2026-06-25 — F3 completada: ingesta histórica ~1 año (2025-06-30→2026-06-25) para cartera de 10 tickers (AAPL,MSFT,NVDA,GOOGL,META,AMZN,TSLA,JPM,GS,QCOM). SQLite en data/processed/radar.db, tablas: news (id=sha256(url||headline)[:16], ticker, datetime, headline, summary, source, url, sentiment_label, sentiment_score) + prices (ticker, date, OHLCV). 29.028 artículos + 251 días×10 precios. Clasificación con DeBERTa-v3-base fine-tuned (F2), cargado en GPU una sola vez. Limitación conocida: Finnhub free tier devuelve mucho news genérico de mercado bajo varios tickers a la vez → duplicados por url+headline (INSERT OR IGNORE); artículos compartidos quedan atribuidos al primer ticker. Profundidad real: ~1 año (máximo del free tier). Rate limit: 1.1s entre llamadas Finnhub (60/min).
- 2026-06-25 — F4 completada: extracción de eventos (gpt-4o-mini, batch=10, 8 tipos: earnings/guidance/m_and_a/leadership/legal/product/analyst/macro/other), tabla events en SQLite. RAG con sentence-transformers all-MiniLM-L6-v2 + FAISS IndexFlatIP (cosine). Briefing markdown generado en reports/briefings/. Test NVDA 7 días: 214 artículos → 36 eventos (28 materiales) → informe completo.
- 2026-06-25 — Cartera corregida: DEFAULT_TICKERS actualizado a AAPL,NVDA,MSFT,AMZN,META,GOOGL,TSLA,JPM,XOM,PFE (GS y QCOM reemplazados por XOM y PFE). XOM y PFE ingeridos retrospectivamente (~1 año). BD contiene 27.266 artículos únicos. GS/QCOM permanecen en la BD como datos históricos.
- 2026-06-25 — Filtro de relevancia completado: keyword pre-filter (TICKER_KEYWORDS) implementado en relevance.py + ingest.py. 11.557 artículos relevantes / 27.266 totales (42%). Piloto LLM 50 artículos: 0/50 rescatados, coste $0.00107; coste extrapolado full pass $0.24. Decisión: no se hace LLM pass (ROI nulo según piloto). Evidencia en reports/results/relevance_pilot.json.
- 2026-06-25 — F5 iniciada: backtest de alerta temprana. Señal diaria de sentimiento (src/backtest/signal.py): solo is_relevant=1, asignación a día de trading (cutoff 20:00 UTC), net score promediado + confianza ponderada + MA3/MA5. Retornos forward en src/backtest/returns.py. Tres análisis en notebooks/04_backtest.ipynb: lead-lag, event study, caso de estudio visual TSLA+NVDA.
- 2026-06-29 — Definición de alerta corregida: alert = (net_sentiment_7d < -0.3) OR (>=1 evento legal material de artículo is_relevant=1). Se excluyen 'guidance' (puede ser positiva, LLM no infiere dirección) y 'macro' (genérico, no específico de empresa). Solo 'legal' es fiablemente negativo. Query de eventos usa JOIN con news para exigir n.is_relevant=1. briefing.py corregido: extracción de eventos solo sobre artículos is_relevant=1 (evita que noticias de Micron/Qualcomm atribuidas a NVDA generen eventos falsos). Contaminación existente en BD: 8 eventos de artículos irrelevantes sobre 36 totales (22%), todos históricos; el JOIN los excluye en runtime.
- 2026-07-01 — F6.2 rehecho: frontend Streamlit sustituido por HTML+CSS+JS vanilla (src/app/frontend/). Razón: control total del diseño vs. look genérico de Streamlit. Estética Stripe/Linear (Inter, fondo claro, acento #635BFF, tarjetas con sombra mínima). Servido desde la propia FastAPI (GET / → index.html, mount /static → frontend/); un único proceso uvicorn en :8000. Chart.js 4 + marked.js 4 por CDN. Streamlit sigue instalado pero ya no forma parte del producto.
- 2026-07-01 — F6.3 completado: dockerización. Imagen base python:3.12-slim. torch instalado con --index-url https://download.pytorch.org/whl/cpu (wheel CPU, no CUDA): el fine-tuning ya está hecho, la inferencia en prod clasifica pocos textos por petición y la CPU es suficiente; la imagen CPU es ~1.5 GB menor. Modelo copiado sin artefactos de entrenamiento (optimizer.pt, scheduler.pt, rng_state.pth, trainer_state.json, training_args.bin excluidos por .dockerignore: ahorran ~600 MB). BD montada como volumen :ro (no se bake datos en la imagen). settings.py limpiado: CORS abierto en dev y prod (frontend co-located, peticiones same-origin). requirements-prod.txt separa deps de runtime (sin streamlit, jupyter, datasets, plotly, etc.).
- 2026-08-24 — Limitación conocida: sesgo negativo por summaries cortos de Finnhub. Clasificador recibe headline+summary (ingest.py); summaries del free tier tienen mediana 145 chars, media 211, 66% bajo 200 chars. En artículos con tesis alcista, el fragmento corto suele capturar solo el contexto de caída del primer párrafo, no la conclusión. DeBERTa clasifica correctamente el texto que recibe; el fallo está en el dato, no en el modelo (macro-F1=0.981 en Financial PhraseBank se mantiene). Estimación sobre muestra n=20 negativos de META: ~15-20% mal etiquetados (caveat: muestra pequeña, no extrapolable con precisión). Efecto: "Termómetro de sentimiento" del briefing puede estar inflado ~5-8 pp en negativo. No afecta a briefings (LLM usa titulares completos). Sin acción correctiva: obtener cuerpo completo requeriría fuente de pago o scraping, fuera de alcance. Sub-hallazgo descartado: artículos multi-ticker entre relevantes META = 8/1149 (0.7%), marginal.
- 2026-08-24 — Prompt de briefing reescrito a español estructurado. Nuevo formato: Veredicto / Termómetro de sentimiento / Temas clave / Señales de riesgo / Fuentes principales. Reglas de deduplicación y agrupación por tema (un hecho = una entrada). Cabecera del markdown en español (Periodo, Generado, Artículos analizados). Regla específica para el juicio federal de Meta: cifra SIEMPRE como "hasta 1,4 billones (máximo teórico legal)", La cifra realista (~200.000 M$) y la naturaleza consultiva del jurado NO se incluyen en el prompt porque no están en los summaries de Finnhub; se aportarán verbalmente en el vídeo. El prompt solo instruye el matiz 'máximo teórico legal' para no presentar el techo como condena esperada. Etiquetas de sentimiento forzadas a español. Briefings regenerados para los 10 tickers.
- 2026-08-18 — Reingesta 2026-07-20 a 2026-08-18: 3413 articulos nuevos (GPU+Finnhub), nueva fecha de corte 2026-08-18. Limite detectado: Finnhub free tier devuelve max ~500 art/llamada mensual; tickers alto volumen (AAPL,NVDA,MSFT,AMZN,META,GOOGL) solo cubren desde ~2026-07-30 (gap 10 dias no recuperable sin plan de pago). Briefings regenerados (10 OK). Alertas activas en ventana 7d: AAPL, AMZN, META, GOOGL, TSLA, PFE (todos por eventos legales; ninguno por sentimiento < -0.3).
- 2026-07-19 — F6.6 snapshot date anchor: /portfolio y /company/{ticker} en api.py ahora calculan la ventana de N dias desde MAX(news.datetime) en lugar de date.today(). Sin este cambio, el snapshot historico mostraba la cartera toda a 0 al pasar el tiempo.
- 2026-07-19 — Reingesta 2026-07-16 a 2026-07-20: 1229 articulos nuevos (GPU+Finnhub), nueva fecha de corte 2026-07-20. Briefings regenerados para los 10 tickers con los datos frescos.
- 2026-07-19 — F6.6 ajuste UX informe: selector de dias eliminado de la pestana Informe (ventana fija 7 dias, unico briefing pre-generado por ticker). Encabezado "Resumen de los ultimos 7 dias" anaido para dejar claro el periodo. CSS muerto del slider quitado de styles.css.
- 2026-07-18 — F6.6 (briefings pre-generados): plan free de Render (CPU lenta, poca RAM) da 502 al generar informe en vivo (extraccion de eventos + RAG + sintesis sobre ~150 articulos). Solucion: pre-generar todos los informes en local (GPU + OPENAI_API_KEY) y guardarlos en tabla briefings dentro de radar.db (viaja en el snapshot). api.py ahora solo lee la BD; sin llamadas a OpenAI en produccion. briefing.py recibe parametro as_of para anclar la ventana temporal al max_date del snapshot. Frontend: informe se auto-carga al abrir empresa (instantaneo), sin slider ni espera visible.
- 2026-07-18 — F6.5 (modelo en HF Hub): DeBERTa fine-tuneado publicado en Rhulli/financial-news-radar-deberta (público, 4 ficheros: config, safetensors, tokenizer x2). Dockerfile: COPY del modelo sustituido por RUN que descarga y cachea en ~/.cache/huggingface durante el build. Estrategia build-time elegida frente a download-on-first-request porque Render free tier reinicia el contenedor en cada cold-start (15 min idle); descargar 750 MB en cada wake-up sería inaceptable. sentiment.py usa MODEL_ID con fallback a checkpoint local (dev).
- 2026-07-15 — F6.4 (HF Spaces): Dockerfile adaptado para HF Spaces: usuario no-root UID 1000 (useradd -m -u 1000 user), WORKDIR /home/user/app, EXPOSE/CMD en puerto 7860 (${PORT:-7860}). docker-compose.yml actualizado (port mapping y rutas de volúmenes). README.md con frontmatter YAML (sdk: docker, app_port: 7860). api.py: briefing endpoint captura EnvironmentError → HTTP 503 en lugar de 500, para que la app arranque y sirva el dashboard sin OPENAI_API_KEY.
- 2026-07-15 — F6.4 completado: imagen Docker autocontenida. radar.db (19 MB) se copia dentro de la imagen en build (.dockerignore cambiado de data/ a data/ + !data/processed/radar.db). Razón: despliegue en plataformas sin host volumes (HF Spaces, Fly.io). En dev, docker-compose.yml monta la BD local sobre el snapshot para reflejar la última ingesta sin rebuilds. Frontend: subtitle corregido ("datos históricos" en lugar de "en tiempo real"); header muestra "Demo · datos hasta <fecha>" en lugar de "Datos a <fecha>".
- 2026-07-01 — F6.2 completado: pestaña Eventos condicional en drill-down: si la empresa no tiene eventos en BD, el tab button se oculta (display:none) al renderizar; showCompany() restaura todos los buttons a visible antes de cargar, evitando estado pegado al navegar entre empresas. Solo NVDA tiene eventos extraídos (36); el resto muestran 3 pestañas.
- 2026-07-01 — F6.2 ajustes vista de cartera: (a) bug cartera vacía corregido: deseleccionar todo muestra estado vacío ("Selecciona una o más empresas…") sin repoblar automáticamente; stats muestran 0/—/—. (b) alturas de tarjeta igualadas: razón de alerta eliminada del interior de la tarjeta. (c) razón como tooltip: data-tooltip en badge ⚠ Alerta, tooltip flotante JS (event-delegated, nodo único en body, posicionado dinámico). (d) texto de alerta en lenguaje natural: "Sentimiento muy negativo: +X.XX de media en N noticias (últimos Nd)" / "Evento legal detectado: …".
- 2026-07-01 — F6.2 mejoras vista de cartera: (a) período configurable 7/14/30d — GET /portfolio acepta ?days=N, frontend tiene segmented control en filter bar, etiquetas dinámicas ("SENTIMIENTO MEDIO 14D", etc.); (b) escala de color del sentimiento con dead zone ±0.03 (antes ±0.1) → valores levemente negativos muestran rojo tenue, no gris; (c) barra de sentimiento con fondo two-tone sutil (rojo izq./verde dcha.) para visualizar el 0 como centro; (d) tarjetas en alerta con borde izquierdo rojo 3px (acento visual inmediato) y badge de alerta más visible; (e) hover de tarjeta reforzado (borde acento azul + translateY −2px); (f) grid 5 columnas fijo en desktop (2 filas limpias para 10 empresas, sin huérfanas); (g) "Datos a YYYY-MM-DD" en cabecera desde max(date_to) del catálogo.
- 2026-06-29 — F6.2 completado: frontend Streamlit (src/app/dashboard.py). Sidebar con multiselect dinámico desde /catalog. Vista de cartera: tarjetas 2-col (dark theme), alertas en rojo arriba, barra de sentimiento visual (█░). Drill-down por empresa: 4 pestañas (Sentimiento con gráfico Plotly + línea umbral, Noticias con enlaces, Eventos con badges de color por tipo, Informe on-demand vía /briefing). Instalados streamlit==1.58 y plotly==6.8 en .venv. Lee API_BASE_URL del entorno (fallback http://127.0.0.1:8000). Inicio: uvicorn en un terminal + streamlit run src/app/dashboard.py en otro.
- 2026-06-29 — F6.1 completado: backend FastAPI. settings.py con clase Settings (dev vs prod: CORS abierto vs restringido a Streamlit, host 127.0.0.1 vs 0.0.0.0). api.py con 5 endpoints: /health, /catalog (dinámico por BD), /portfolio?tickers= (alerta si net_sentiment<-0.3 o evento material negativo), /company/{ticker} (serie 30d + 20 noticias + eventos), /company/{ticker}/briefing (llama a F4 en thread pool). Catálogo dinámico: ampliar la BD amplía el catálogo sin tocar código. fastapi[standard]+uvicorn instalados en .venv.
- 2026-06-23 — F2 LLM zero-shot completado: gpt-4o-mini, temperature=0, structured JSON output, zero-shot (sin ejemplos etiquetados). Test macro-F1=0.963 ($0.0108 USD por 339 frases, ~1 céntimo). F1 negative=0.979 (recall=1.000), neutral=0.971, positive=0.939. Patrón de errores: sesgo conservador hacia neutral en noticias positivas ambiguas (8/12 errores son positive→neutral). Clave leída de OPENAI_API_KEY en .env. Resultados en reports/results/llm_zeroshot.json. F2 COMPLETADA.
